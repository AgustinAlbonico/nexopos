# Frontend UI contract

S3 reuses the existing shadcn/Tailwind sales surface: `Dialog` with `max-w-xl`,
rounded bordered item rows, `Button` variants for secondary/primary actions, and
`text-muted-foreground` for supporting copy. New flows must preserve the current
primary-color action treatment and use Lucide icons only.
